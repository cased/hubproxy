// Package storage provides interfaces and types for storing and retrieving GitHub webhook events.
// It supports multiple storage backends through a common interface. The main types and
// interfaces are defined in types.go.
package storage
